MISTRAL_API_KEY=
WHISPER_MODEL=small

# Comma-separated list of origins allowed to call the FastAPI backend.
# Defaults to the Vite dev server (5173) and CRA-style dev servers (3000).
FRONTEND_ORIGIN=http://localhost:5173,http://localhost:3000
